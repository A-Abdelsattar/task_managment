import 'package:flutter/material.dart';


class ViewTaskWithDateScreen extends StatelessWidget {
  const ViewTaskWithDateScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [

        ],
      ),
    );
  }
}
